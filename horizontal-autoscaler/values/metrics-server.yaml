---
defaultArgs:
  - --cert-dir=/tmp
  - --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname
  - --kubelet-use-node-status-port
  - --metric-resolution=15s # you also need to specify metric resolution to 15s
  - --secure-port=10250 # you need to specify secure port as 10250